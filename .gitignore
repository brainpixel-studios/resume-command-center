# Secrets — never commit
.env
.env.local

# Dependencies / build output
node_modules/
dist/
.tmp/
__pycache__/
.venv/
*.pyc

# Editor / OS junk
*.bak
.DS_Store

# Any real résumé data (belongs in localStorage, never the repo)
real.json
*.real.json
